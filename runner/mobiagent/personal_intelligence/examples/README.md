# Examples

Small, sanitized examples for Personal Intelligence ingestion and workflow collection.

`workflows/android_vlm_qa_synthetic_demo.json` is a minimal Stage 2 example for
the optional `workflow_vlm_qa_output` provider. It uses a synthetic demo page and
expects the workflow `vlm_qa` tool to provide `structured_output.visible_text`.
